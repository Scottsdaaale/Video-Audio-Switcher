function ControlBar() {
  return (
    <div
      style={{
        color: "white",
        padding: "10px",
        borderRadius: "26px",
        background: "rgba(0, 0, 0, 0.40)",
        backdropFilter: "blur(14px)"
      }}
    >
      ControlBar
    </div>
  );
}

export default ControlBar;
